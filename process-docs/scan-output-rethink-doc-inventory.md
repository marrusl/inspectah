# Scan Output Rethink — Doc Update Inventory

Generated by T9a grep. Consumed by T9b for post-snapshot finalization.

## Files Requiring Updates

| File | Stale references | Update type |
|------|-----------------|-------------|
| docs/reference/cli.md | `--progress <MODE>` table with `rich`, `plain`, `flat` | Rewrite mode section to `pretty`/`flat` |
| docs/reference/configuration.md | `INSPECTAH_PROGRESS` values `rich`, `plain`, `flat` | Update env var values to `pretty`/`flat` |
| docs/reference/configuration.md | Mode comparison table with `rich`/`plain` rows | Rewrite table for `pretty`/`flat` |
| docs/reference/configuration.md | Default fallback logic mentions `rich` | Update to `pretty` |
| docs/reference/configuration.md | CLI flag example with `rich`, `plain`, `flat` | Update to `pretty`/`flat` |
| docs/how-to/customize-output.md | Example `--progress rich` | Update to `--progress pretty` |
| docs/how-to/customize-output.md | Text "inspectah falls back to plain mode automatically" | Update to "flat mode" |
| docs/how-to/customize-output.md | Example `--progress plain` | Remove or replace with `--progress flat` |
| docs/explanation/architecture.md | Progress module description mentions `rich.rs` and `plain.rs` | Update to `pretty.rs` and `flat.rs` |
| README.md | `--progress <MODE>` description with `rich`, `plain`, `flat` | Update to `pretty`/`flat` |
| README.md | `INSPECTAH_PROGRESS` env var values | Update to `pretty`/`flat` |

## Non-Stale Grep Matches (False Positives)

These matches contain "plain", "rich", or "explaining" but are NOT stale references:

- docs/contributing/adding-an-inspector.md:32 — `pub rich_rules: Vec<String>` (code field, unrelated)
- Multiple files with "explaining why" or "explains" — prose, not mode references

## Verification Steps (for T9b)

- [ ] Reconcile `scan --help` output against updated docs
- [ ] Compare transcript snippets against accepted T8 snapshots
- [ ] Final stale-reference grep (zero matches for `--progress rich|plain` outside CHANGELOG)
- [ ] Verify INSPECTAH_PROGRESS env var values match actual implementation
- [ ] Check that all mode comparison tables reflect pretty/flat only

## Notes

The architecture.md file mentions backend module filenames (`rich.rs`, `plain.rs`) which may have been renamed in the refactor. T9b should verify actual module names before updating.
